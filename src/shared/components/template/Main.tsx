import React from "react";

const Main: React.FC<{ children: React.ReactNode }> = ({ children }) => {
    return (
        <main>
            <h1>MAIN</h1>
        </main>
    );
}

export default Main;